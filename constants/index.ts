import { IFormResponse } from '@/types';

export const defaultFormState = {
    email: '',
    name: '',
    password: ''
};

export const defaultFormResponse: IFormResponse = {
    type: 'success',
    message: ''
};

export const DEFAULT_USER_IMAGE = '/images/default-red.png';

export const TOP_OFFSET = 66;

